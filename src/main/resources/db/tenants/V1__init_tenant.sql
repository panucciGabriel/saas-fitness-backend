CREATE TABLE students (
                          id BIGSERIAL PRIMARY KEY,
                          name VARCHAR(255),
                          email VARCHAR(255),
                          plan VARCHAR(255)
);